package model

// should not do this on real application, save the configuration on separate config file
const (
	DBHost     = "localhost"
	DBPort     = 5432
	DBUser     = "toped"
	DBPassword = "toped"
	DBName     = "todo-db"
)
